ASSUME CS:CODE, DS:DATA
DATA SEGMENT
LIST DB 22H,11H,0BBH,7BH,88H
CNT EQU 04H
SUM DW ?
DATA ENDS
CODE SEGMENT
START:MOV AX,DATA
      MOV DS,AX
      XOR AX,AX
      MOV CX,CNT
      MOV SI,OFFSET LIST
      MOV AL,[SI]
BACK:INC SI
     ADD AL,[SI]
     ADC AH,00H
     LOOP BACK
     MOV SUM,AX
     MOV AH,4CH
     INT 21H
CODE ENDS
END START
